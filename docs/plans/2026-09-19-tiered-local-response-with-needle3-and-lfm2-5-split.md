# Implementation Split Plan — Tiered local response with Needle3 and LFM2.5

slug: `tiered-local-response-with-needle3-and-lfm2-5` · generated by `assign-to-workforce.sh split-plan --write` from `devague plan waves --json` (issue #82). Re-running this command overwrites this file in place; edits made to the **Task assignments** table below survive a regeneration, matched by task id.

## Dependency waves

- Wave 1: `t1`, `t4`, `t6`
- Wave 2: `t2`, `t3`, `t5`, `t7`, `t8`, `t16`
- Wave 3: `t9`, `t10`, `t11`, `t15`, `t17`, `t18`, `t20`
- Wave 4: `t12`, `t14`, `t21`, `t22`
- Wave 5: `t13`, `t19`, `t23`
- Wave 6: `t24`, `t25`

## Task assignments

Edit the Owner/Model columns before approving gate 2 — the default Model is a presentation-only proposal (`sonnet`), never a recommendation. Re-running `split-plan --write` preserves your edits here, matched by task id.

| Task | Owner | Model |
| --- | --- | --- |
| `t1` | qwen CLI in worktree | worker |
| `t4` | qwen CLI in worktree | worker |
| `t6` | qwen CLI in worktree | worker |
| `t2` | Claude subagent | sonnet |
| `t3` | qwen CLI in worktree | worker |
| `t5` | Claude subagent | sonnet |
| `t7` | qwen CLI in worktree | worker |
| `t8` | Claude subagent | sonnet |
| `t16` | qwen CLI in worktree | worker |
| `t9` | Claude subagent | opus |
| `t10` | qwen CLI in worktree | worker |
| `t11` | Claude subagent | opus |
| `t15` | Claude subagent | sonnet |
| `t17` | Claude subagent | opus |
| `t18` | Claude subagent | opus |
| `t20` | Claude subagent | sonnet |
| `t12` | Claude subagent | opus |
| `t14` | Claude subagent | sonnet |
| `t21` | Claude subagent | sonnet |
| `t22` | Claude subagent | sonnet |
| `t13` | Claude subagent | opus |
| `t19` | Claude subagent | sonnet |
| `t23` | qwen CLI in worktree | worker |
| `t24` | main agent + operator | fable |
| `t25` | main agent + operator | fable |

## Wave 1

### t1 — Operation table model and validation: `nvsh/ops/__init__.py`, `nvsh/ops/_model.py`, `nvsh/ops/table.py`

- instruction: Pure data + pure functions, no subprocess. Model on nvsh/platform/`_model.py`'s frozen dataclasses. Operation names: `machine_status`, `memory_stats`, `gpu_stats`, `disk_stats`, `thermal_stats`, `container_list`, `network_info`, `process_list`, `power_get`, `swap_status`, `nvsh_doctor` (read-only); `power_set`, `service_status`, `service_logs`, `service_restart`, `container_restart`. `service_status` and `service_logs` are read-only.
- covers: c8, c9, h7
- acceptance:
  - tests/`test_ops_table.py`: the table holds the 16 operations named in c8, each with typed arguments and a `read_only`/mutating flag
  - validate(name, args) returns a typed error for an unknown operation, a missing/extra argument, a wrong type, or a Literal value outside its set; never raises
  - nvsh/ops imports nothing outside the stdlib and tests/`test_import_time.py` still passes

### t4 — Tier measurement records with rotation: `nvsh/tiers/__init__.py`, `nvsh/tiers/records.py`

- instruction: Follow nvsh/agent/audit.py's conventions (dir 0700, file 0600, `sort_keys`). Rotation: numbered files, size cap from config with a default of 8 MB total.
- covers: c6, c35, h30
- acceptance:
  - tests/`test_tier_records.py`: a record carries tier, selected operation, confidence (may be None), `latency_ms`, decline reason, `escalated_to`, operator decision; written as JSONL under `XDG_STATE_HOME`/nvsh with 0600 perms
  - every record passes through nvsh/redact.py; the raw request text is stored only when config opts in, otherwise only its length
  - writing past the configured cap rotates the oldest file out; total size stays under cap

### t6 — Config `[tiers]` table: `nvsh/config.py`, docs/config.example.toml

- instruction: Add 'tiers' to `_VALID_TOP_KEYS` and follow `_apply_triggers`. Routing stays off unless enabled = true (decision c22).
- covers: c14, c10, c33
- acceptance:
  - tests/`test_config.py`: `[tiers]` parses enabled (default false), decline thresholds, `memory_floor_mb`, `idle_unload_seconds`, records cap, `store_request_text` (default false), and `[tiers.lfm]` engine (llama-server|vllm|sglang), mode (managed|attach), `base_url`
  - unknown keys fail loudly via the existing `_reject_unknown` path; save() round-trips the table; `default_toml`() documents it

## Wave 2

### t2 — Per-platform rendering of operations to argv, plus `thor`/`orin` CLI detection: `nvsh/ops/render.py`, `nvsh/platform/_detect.py`, docs/platforms.md

- instruction: Verb map from scope s14: status, memory, gpu, disk, thermal, containers, network, processes, swap exist on spark and thor; power only on thor; orin has none yet. Detect verbs by a static per-CLI table, not by running --help at request time.
- covers: c8, h7, h20
- acceptance:
  - tests/`test_ops_render.py` table-tests every operation per platform kind with the device CLI present (`spark`/`thor`/`orin` `<verb> --json`) and absent (system command), asserting the exact argv list
  - render() returns a list of strings, never a shell string; a verb the detected device CLI lacks falls back to the system command
  - platform detection reports `thor` and `orin` on PATH the way it reports `spark`, with source recorded in docs/platforms.md

### t3 — Argument grounding: `nvsh/ops/ground.py`

- instruction: Lookups: `systemctl list-units --type=service --all --no-legend --plain` and `docker ps -a --format {{.Names}}`, both read-only, short timeout, injected for tests. An ambiguous match (two candidates) is a decline listing them.
- covers: c30, h25
- acceptance:
  - tests/`test_ops_ground.py`: `rm -rf /`, `x; reboot`, `$(id)` and a non-existent unit all return a decline with a one-line message
  - 'vLLM' resolves case-insensitively to an existing `vllm.service` fixture; a container argument resolves only to a name the lookup returned
  - lookups run through an injected runner with argv lists; a test asserts no call uses shell=True

### t5 — Tier contract, decision validation and fixture tier: `nvsh/tiers/base.py`, `nvsh/tiers/fake.py`

- instruction: TierDecision = operation + args + confidence | Decline(reason). Reason codes are an enum reused by records. FakeTier replays scripted outputs like nvsh/agent/fake.py.
- covers: c9, h8, c29, h24
- acceptance:
  - tests/`test_tier_base.py`: a fixture tier output naming an unknown operation, a bad argument, or a raw shell string each becomes a decline with a reason code
  - zero calls or more than one call is a decline; exactly one valid call becomes a TierDecision
  - a Tier has select(request, context) and close(); no tier type has an execute method

### t7 — Memory floor check: `nvsh/tiers/memfloor.py`

- instruction: Read MemAvailable from /proc/meminfo via an injected reader. No psutil.
- covers: c33, h28
- acceptance:
  - tests/`test_tier_memfloor.py`: with a faked /proc/meminfo below the floor the check returns a decline and one status line; above the floor it passes; an unreadable meminfo passes with a note

### t8 — Pinned fetch and prefetch: `nvsh/tiers/fetch.py`, `nvsh/tiers/pins.json`

- instruction: Plan/execute split like nvsh/installers.py: planning is pure, execution is one function. Image pulls are `docker pull <image>@sha256:...`. Avoid cactus-needle's `force_download` counter call by resolving from the local cache path directly (scope s15).
- covers: c12, h11, c31, h26
- acceptance:
  - tests/`test_tier_fetch.py`: a file whose sha256 differs from pins.json is refused and reported; a matching file is accepted
  - `plan_prefetch`() lists each item (engine, weights, container image by digest) with its size before anything downloads; after prefetch, resolve() makes no network call (asserted with a socket guard)
  - pins.json is listed in both pyproject include and artifacts so the wheel ships it

### t16 — Tool-call chat client over stdlib HTTP: `nvsh/tiers/toolchat.py`

- instruction: urllib only. Reuse the socket-shutdown stop path from nvsh/agent/`openai_compat.py`. The raw fallback covers plan risk on per-engine tool-call parsing.
- covers: c25, h18
- acceptance:
  - tests/`test_tier_toolchat.py` against a fixture HTTP server: parses OpenAI `tool_calls` (streamed and non-streamed), and falls back to parsing a raw tool-call block in content when `tool_calls` is absent
  - only <http://127.0.0.1> or localhost base URLs are accepted; pointing at a different fixture port is a config change only

## Wave 3

### t9 — Needle3 child-process tier: `nvsh/tiers/needle.py`, `nvsh/tiers/needle_worker.py`

- instruction: Parent speaks length-prefixed JSON over the child's stdin/stdout. Child calls needle.Needle(tools=...).complete() with reset() between requests. Tool schemas are generated from the operation table, with function bodies that do nothing.
- covers: c32, h27, c12, h11, c9, h8
- acceptance:
  - tests/`test_tier_needle.py` with a fake worker: killing the child mid-request yields a decline and the next request restarts the child; a hung child is killed after a timeout
  - the worker sets `NEEDLE_TELEMETRY`=0, `DO_NOT_TRACK`=1 and `HF_HUB_OFFLINE`=1 before importing needle (asserted by test); a grep-style test asserts nvsh never references Needle.run
  - with cactus-needle not installed the tier reports unavailable with one status line and nothing raises

### t10 — Install flavors: pyproject.toml extras `needle`, `lfm`, `tiers`

- instruction: Keep tests/`test_import_time.py` green: no tier module may be imported by nvsh.cli startup or the hook path.
- covers: c11, h10
- acceptance:
  - dependencies stays \[\]; \[project.optional-dependencies\] defines needle (pinned cactus-needle range), lfm (empty or marker-only, since the runtime is a container) and tiers (both)
  - tests/`test_wheel_packaging.py`: a no-extras install imports and runs every existing verb; a test with a deliberately broken needle import shows the request reaching the full agent with one status line

### t11 — Tier router: `nvsh/tiers/router.py`

- instruction: The router yields AgentEvents so the client can stream it like any adapter. ProposalKind: INSPECT for read-only operations, FIX for mutating. Escalation context = list of (operation, redacted result excerpt).
- covers: c7, h6, c10, h9, c17, h16
- acceptance:
  - tests/`test_tier_router.py` with fixture tiers: answered at Tier 1; declined by 1 and answered at 2; declined by both and handed to Tier 3 — each writes a record naming the tier and decline reason
  - a FAILURE request never reaches Tier 1; an explicit request tries Tier 1 first
  - a wrong mutating operation at confidence 1.0 becomes a Proposal whose rationale shows the interpreted operation and arguments and nothing executes without approval; the same passes with confidence None
  - the router returns Proposals built from nvsh/ops render output only; existing approval, sudo and destructive-command tests pass unchanged with tiers enabled

### t15 — Doctor checks for tiers: `nvsh/doctor_checks.py`

- instruction: Add a reachability branch for needle and lfm in `check_agent_reachable` so they leave the 'no reachability probe' warning.
- covers: c31, h26
- acceptance:
  - tests/`test_doctor_checks.py`: checks `tiers_configured`, `tier_files_present` and `tier_hashes_match` in the rubric shape; a swapped file fails `tier_hashes_match` with a remediation naming the prefetch command
  - with no flavor installed doctor reports one info check and stays healthy

### t17 — Docker runtime launcher for Tier 2: `nvsh/tiers/runtime_docker.py`, docs/platforms.md

- instruction: Per-engine argument templates for llama-server, vllm, sglang. CPU fallback when no GPU path is detected. Stop and remove the container on idle unload and daemon exit.
- covers: c25, h23, c37, h32
- acceptance:
  - tests/`test_tier_runtime_docker.py`: the rendered launch line uses `--gpus all` on a dgx-spark platform and `--runtime nvidia` on jetson, binds 127.0.0.1 only, and names the image by @sha256 digest
  - with Docker missing, or the daemon socket unreachable, the tier declines with one status line; attach mode starts nothing
  - a grep-style test asserts this is the only `docker run` in nvsh and its arguments come from config and detection only
  - container name and port are derived per OS user so two users do not collide

### t18 — Tier 2 bounded loop: `nvsh/tiers/lfm.py`

- instruction: Context budget is fixed (system brief + failure + last N tool results). The system prompt lists operations from the table; escalate is a tool the model can call.
- covers: c15, h14
- acceptance:
  - tests/`test_tier_lfm.py` with a fixture chat server: a model that never stops inspecting is cut off at 4 rounds and escalates
  - a mutating operation requested inside the loop is refused and fed back as an error; read-only operations run through nvsh/ops render with a timeout and a bounded, redacted result
  - outcomes are exactly propose | explain | escalate, and the escalated request carries the inspection results

### t20 — `nvsh tiers` CLI verbs — stats, export, prefetch: `nvsh/cli/_commands/tiers.py`, `nvsh/explain/catalog.py`

- instruction: Follow the CLI contract in CLAUDE.md: CliError, `emit_result`, register(sub).
- covers: c6, h5, c12, c17, h16
- acceptance:
  - tests/`test_cli_tiers.py`: `nvsh tiers stats [--json]` prints per-tier counts, latency p50/p95, escalation reasons and approve/decline rates from local records
  - `nvsh tiers export <file>` writes a redacted bundle to a local path only and opens no socket; `nvsh tiers prefetch` shows sizes and asks before downloading
  - every verb supports --json, has an explain catalog entry, and teken cli doctor --strict passes

## Wave 4

### t12 — Daemon residency: tiers in `nvsh/daemon.py`

- instruction: Hook after `request_from_dict` in `handle_message`, before `_run`. Keep the tier manager in nvsh/tiers; daemon.py only wires it. Document the wire kind in docs/daemon.md.
- covers: c13, h12, c32, h27
- acceptance:
  - tests/`test_daemon_tiers.py`: tier models load on first use, not at daemon start, and unload after `idle_unload_seconds`
  - a Tier 1 answer returns while another shell holds `_run_lock`
  - a new 'tier' wire kind lets the one-shot client ask the daemon for a tier decision; with the daemon stopped the client falls through to the full agent without loading a model

### t14 — Explicit `needle` adapter: `nvsh/agent/needle.py`, `nvsh/agent/registry.py`, tests/`_fake_adapters.py`

- instruction: The adapter wraps the router restricted to Tier 1. Add 'inproc' to `PATH_VALUES`. `__init__` must stay cheap and spawn nothing.
- covers: c14, h13
- acceptance:
  - `@needle` and `nvsh --agent needle` work when the flavor is installed; the adapter joins tests/`test_agent_conformance.py`
  - needle is in `PROBE_EXCLUDED`: a test asserts nvsh setup never offers it as default, and nvsh agent use needle is refused like demo
  - nvsh agent list --json reports it with `local_model`=True, approval='nvsh', `unmediated_file_access`=False

### t21 — Uninstall removes tier state: `nvsh/cli/_commands/setup.py`

- instruction: Only touch `cmd_uninstall` and its helpers.
- covers: c35, h30
- acceptance:
  - tests/`test_setup.py`: uninstall on a box with tier records, prefetched files and a running Tier 2 container leaves none of nvsh's own files, stops and removes the container, and prints which images remain and how to remove them

### t22 — Benchmark corpus and runner: `nvsh/tiers/bench.py`, `nvsh/tiers/corpus/*.json`, `nvsh tiers bench`

- instruction: Memory from `/proc/<pid>/status` and docker stats. Held-out phrasings must not be authored in the same sitting as operation descriptions — leave held-out.json with a header asking the operator to add or review entries (assumption c38).
- covers: c16, h15, c34, h29, c26, h19, c20, h17
- acceptance:
  - the corpus holds the prompts and failure cases from issues #30/#31 and scope s13/s16, each with expected operation+arguments or expected escalation, split into dev and held-out files
  - tests/`test_tier_bench.py`: running twice with a fixture tier gives identical accuracy; the results file records model file hashes, nvsh version, device, engine, CPU/GPU mode, concurrent load
  - results include accuracy, argument accuracy, false-mutating-pick count, escalation precision/recall, cold and warm latency, idle/peak/reserved memory, image size, and a pass/miss line for each c20 target

## Wave 5

### t13 — Client routing and panel tier header: `nvsh/client.py`, `nvsh/client_transport.py`, `nvsh/panel.py`

- instruction: Insert in front of `_stream_request`'s `_send`. Reuse the existing header slot that shows the target.
- covers: c14, h13, c36, h31
- acceptance:
  - tests/`test_client_tiers.py`: with \[tiers\] enabled = false no tier is consulted; with it true /ask and Ctrl+G consult tiers; `@claude` sends the request past both tiers
  - the panel header names the tier that answered (needle, lfm, or the full agent's name), including the escalated case
  - declining a tier proposal lets the operator send the same request to the full agent

### t19 — Explicit `lfm` adapter and Tier 2 wiring: `nvsh/agent/lfm.py`, `nvsh/agent/registry.py`, `nvsh/tiers/router.py`

- instruction: Registry edits are small; t14 already added the pattern.
- covers: c14, h13, c7
- acceptance:
  - `@lfm` works, joins the conformance suite, is `PROBE_EXCLUDED` and refused as default
  - the router's Tier 2 slot uses the real LFM tier when the flavor is configured; failures start there

### t23 — Needle3 LoRA fine-tune recipe: `scripts/needle-finetune/`, docs/needle-finetune.md

- instruction: Training runs on a development machine with cactus-needle\[train\]; nothing here is imported by the nvsh package.
- covers: c40, h33
- acceptance:
  - a dataset builder turns the dev split (plus an optional redacted export bundle) into needle finetune JSONL; tests cover the conversion
  - the doc gives the exact `needle finetune` and `needle build --lora` commands, and how to pin the resulting .cact hash in pins.json
  - adoption rule is written down: tuned ships only if it beats stock on held-out correct-operation rate with no more wrong mutating picks

## Wave 6

### t24 — Run the benchmark on DGX Spark, AGX Thor and AGX Orin and commit results: docs/benchmarks/

- instruction: Main agent task, not a subagent: needs ssh thor / ssh orin and the operator's go-ahead before installing anything on those boxes.
- covers: c1, h21, c2, h22, c3, h3, c4, h4, c20, h17, h18, h23, h12, h19, h11, h28, h33
- acceptance:
  - one results file per device is committed; a device or metric not measured is listed as not measured
  - the AGX Orin file has an 'Orin Nano estimate' section with its scaling assumptions, marked estimated; it records the no-full-agent behaviour of h21 and GPU offload evidence for h23
  - at least one device has results for two engines; one device has a timing of the same prompts through today's default full agent; stock and tuned Needle3 are side by side; one run is done with networking blocked
  - every c20 number comes from the bench command; missed targets are reported as missed

### t25 — Docs, prompt files, sibling-repo issues, version bump

- instruction: Filing issues on sibling repos is outward-facing: get the operator's go-ahead, use the communicate skill.
- covers: h20, c8
- acceptance:
  - docs/architecture.md, docs/daemon.md, docs/shell-integration.md and docs/current-spec.md describe the tiers; README has a short section; CLAUDE.md, AGENTS.override.md + .pi/SYSTEM.md, AGENTS.colleague.md and QWEN.md agree; harness-smoke config stage passes
  - issues are filed on jetson-orin-cli (machine verbs), dgx-spark-cli (power verb) and all three (service and container-restart verbs) and linked from the spec's follow-ups
  - version bumped with a CHANGELOG entry; markdownlint and scan-secrets pass

## End state

### Announcement

- nvsh answers routine operator requests and routine failures locally in well under a second: a tiny Needle3 reflex picks a typed deterministic operation, a small resident LFM2.5 agent handles the middle ground, and the configured full agent is called only when neither can — so even a Jetson Orin Nano gets a useful nvsh with no large model at all.

### After state

- An explicit operator request (`/ask`, Ctrl+G, a `?`-marked line) goes through tiers in order: Tier 0 shell and slash commands, Tier 1 Needle3 selecting one typed operation, Tier 2 a small resident LFM2.5 agent running a bounded inspect-interpret-propose loop, Tier 3 the configured full NvshAgent. A qualifying command FAILURE skips Tier 1 and starts at Tier 2, because Tier 1 selects an operation from a short intent and measurably mis-selects (including mutating operations) when shown command/exit/stderr text. Each tier either answers with a normal nvsh Proposal or declines, and a decline moves the request up one tier. A machine with only Tier 1 (or Tiers 1-2) installed is a supported configuration, not a degraded one.

### Success signals

- On the committed corpus, on each benchmarked device (AGX Orin, AGX Thor, DGX Spark): Tier 1 warm selection p95 < 150 ms; >= 90% of fast-path prompts get the correct operation and arguments; 0 wrong mutating operations proposed without the interpreted operation being shown; >= 80% of should-escalate prompts are declined by Tier 1; a command that succeeds still costs one bash function call and 0 forks. Memory: with both flavors resident the daemon adds < 1 GB to system memory as measured on the AGX Orin, which is the stand-in for the Orin Nano's 8 GB budget until a Nano is measured.

### Terminal tasks

- `t10` — Install flavors: pyproject.toml extras `needle`, `lfm`, `tiers`
  - dependencies stays []; [project.optional-dependencies] defines needle (pinned cactus-needle range), lfm (empty or marker-only, since the runtime is a container) and tiers (both)
  - tests/test_wheel_packaging.py: a no-extras install imports and runs every existing verb; a test with a deliberately broken needle import shows the request reaching the full agent with one status line
- `t15` — Doctor checks for tiers: `nvsh/doctor_checks.py`
  - tests/test_doctor_checks.py: checks tiers_configured, tier_files_present and tier_hashes_match in the rubric shape; a swapped file fails tier_hashes_match with a remediation naming the prefetch command
  - with no flavor installed doctor reports one info check and stays healthy
- `t21` — Uninstall removes tier state: `nvsh/cli/_commands/setup.py`
  - tests/test_setup.py: uninstall on a box with tier records, prefetched files and a running Tier 2 container leaves none of nvsh's own files, stops and removes the container, and prints which images remain and how to remove them
- `t24` — Run the benchmark on DGX Spark, AGX Thor and AGX Orin and commit results: docs/benchmarks/
  - one results file per device is committed; a device or metric not measured is listed as not measured
  - the AGX Orin file has an 'Orin Nano estimate' section with its scaling assumptions, marked estimated; it records the no-full-agent behaviour of h21 and GPU offload evidence for h23
  - at least one device has results for two engines; one device has a timing of the same prompts through today's default full agent; stock and tuned Needle3 are side by side; one run is done with networking blocked
  - every c20 number comes from the bench command; missed targets are reported as missed
- `t25` — Docs, prompt files, sibling-repo issues, version bump
  - docs/architecture.md, docs/daemon.md, docs/shell-integration.md and docs/current-spec.md describe the tiers; README has a short section; CLAUDE.md, AGENTS.override.md + .pi/SYSTEM.md, AGENTS.colleague.md and QWEN.md agree; harness-smoke config stage passes
  - issues are filed on jetson-orin-cli (machine verbs), dgx-spark-cli (power verb) and all three (service and container-restart verbs) and linked from the spec's follow-ups
  - version bumped with a CHANGELOG entry; markdownlint and scan-secrets pass

### Open items

- [unknown_nonblocking] Whether stock LFM2.5-230M or 350M can do the Tier 2 role at all is unmeasured; the spike covered Needle3 only. If both fail and 1.2B also fails, Tier 2 is dropped and Tier 1 escalates straight to the full agent.
- [unknown_nonblocking] Needle3 has only been run on the DGX Spark. libneedle.so on the Jetsons' L4T userland (glibc version, tegra234 and tegra264) and on an 8 GB Orin Nano is unverified.
- [follow_up] Fine-tuning Needle3 (LoRA) and an LFM2.5 Base checkpoint on nvsh data, using the records this spec starts collecting.
- [unknown_nonblocking] LFM Open License terms (attribution, commercial revenue threshold) need a read before nvsh documents or prefetches LFM2.5 weights; nvsh would download from the publisher, never redistribute.
- [follow_up] Measure the tiers on a real Jetson Orin Nano once the operator's unit is set up and reachable, and replace the AGX-Orin-derived estimates with the measured numbers.
- [unknown_nonblocking] Whether LFM2.5's tool-call output is parsed into OpenAI `tool_calls` by llama-server, vLLM and SGLang (chat-template / parser support differs per engine and per version) is unverified; if an engine does not, nvsh parses the model's raw tool-call text itself or that engine is not offered.
- [unknown_nonblocking] Two OS users on one machine would each start a daemon and each want a Tier 2 container on localhost; port allocation and container naming per user is undecided.
- [unknown_nonblocking] Needle3's libneedle.so has only been run on the DGX Spark; if it does not load on the Jetsons' L4T userland, Tier 1 on Jetson needs an upstream fix or a different build, and t24 reports those devices as not measured.
- [unknown_nonblocking] No LFM2.5 inference has been run. If stock 230M, 350M and 1.2B all fail the Tier 2 corpus, Tier 2 is dropped per the spec and t18/t19 ship disabled with the results saying why.
- [unknown_nonblocking] Tool-call parsing for LFM2.5 differs per engine and version; t16's raw-content fallback is the mitigation, but an engine may still be unusable.
- [unknown_nonblocking] LFM Open License terms must be read before nvsh documents or prefetches LFM2.5 weights.
- [unknown_nonblocking] 25 tasks is too large for one PR under this repo's every-PR-bumps-version rule. Proposed delivery in three PRs following the phases of decision c21: (A) waves 0-1 operation table, records, config, fetch, flavors; (B) Tier 1 end to end — t9, t11, t12, t13, t14, t15, t20, t22, t23; (C) Tier 2 — t16-t19, t21, then t24/t25. The operator decides the PR split at the workforce gate.
- [unknown_nonblocking] t12 (daemon.py, 1715 lines) and t13 (client.py) edit the two largest, most-changed files while PRs #22 and #29 are still open against main; rebase conflicts are likely. Merge #22 and #29 first.
- [follow_up] Measuring on a real Orin Nano (frame follow-up v5) and LFM2.5 fine-tuning (v3).
