# Implementation Split Plan — stop choice prompt

slug: `stop-choice-prompt` · generated by `assign-to-workforce.sh split-plan --write` from `devague plan waves --json` (issue #82). Re-running this command overwrites this file in place; edits made to the **Task assignments** table below survive a regeneration, matched by task id.

## Dependency waves

- Wave 1: `t1`, `t2`, `t3`, `t10`
- Wave 2: `t4`, `t5`
- Wave 3: `t6`
- Wave 4: `t7`
- Wave 5: `t8`
- Wave 6: `t9`
- Wave 7: `t11`

## Task assignments

Edit the Owner/Model columns before approving gate 2 — the default Model is a presentation-only proposal (`sonnet`), never a recommendation. Re-running `split-plan --write` preserves your edits here, matched by task id.

| Task | Owner | Model |
| --- | --- | --- |
| `t1` | subagent | sonnet |
| `t2` | subagent | opus |
| `t3` | subagent | sonnet |
| `t10` | subagent | sonnet |
| `t4` | subagent | sonnet |
| `t5` | subagent | opus |
| `t6` | subagent | opus |
| `t7` | subagent | sonnet |
| `t8` | subagent | opus |
| `t9` | subagent | opus |
| `t11` | main agent | fable |

## Wave 1

### t1 — Declare Capabilities.steer on every adapter and expose a client-side lookup

- instruction: Files: nvsh/agent/base.py, every adapter under nvsh/agent/, nvsh/agent/registry.py, tests/`test_agent_conformance.py` and the per-adapter capability tests. Do not touch nvsh/daemon.py, nvsh/panel.py or nvsh/client.py. Follow the all-backends rule: all nine adapters declare the field explicitly.
- covers: c5, h5, c31
- acceptance:
  - nvsh/agent/base.py Capabilities gains a bool field steer defaulting to False; pi and codex declare steer=True; acp, claude, qwen (both modes), agy, openai-compat, fake and demo declare False
  - a conformance test iterates registry.ADAPTERS and asserts exactly {pi, codex} declare steer=True, and `test_capability_report` checks the new field is a bool
  - nvsh/agent/registry.py gains `steer_capable`(name, config) built like `_tool_calling`: it constructs the adapter, reads .capabilities().steer and starts no process (asserted with a fake whose start() raises)
  - nvsh agent list --json shows the new capability for every adapter; existing capability equality tests are updated, not deleted

### t2 — Add a timed single-key reader for prompts in a new module

- instruction: Files: NEW nvsh/promptkeys.py and NEW tests/`test_promptkeys.py` only. Reuse nvsh.keys helpers (`_after_esc` / `read_choice_key`) by import; do not edit nvsh/keys.py or nvsh/panel.py. Stdlib only (termios, tty, select). Tests use pty.openpty().
- covers: c32, h23, c33, h24, c7, h7
- acceptance:
  - new module nvsh/promptkeys.py exposes `read_choice`(fd, keys, timeout) returning one of the allowed keys, ESC, INTERRUPT (Ctrl+C byte 0x03), EOF or TIMEOUT, using select() with a monotonic deadline; escape sequences (CSI, SS3) are drained whole and neither answer nor leave bytes behind
  - pending input is discarded (termios.tcflush TCIFLUSH) before the read starts, so two Esc bytes written within 20 ms leave the read unanswered -- asserted on a pty
  - termios is restored on every exit path including exceptions; EOF or a hung-up tty returns EOF at once without spinning; with no key for the timeout it returns TIMEOUT within +/- 1 s for a 2 s test timeout
  - nvsh/keys.py has no diff and tests/`test_keys.py` passes unmodified

### t3 — Add the `keep_going` stop kind and an origin field to the audit log

- instruction: Files: nvsh/agent/audit.py and tests/`test_audit.py` only.
- covers: c13, h13
- acceptance:
  - nvsh/agent/audit.py `STOP_KINDS` gains `keep_going`; the stop writer accepts an optional origin ('`stop_prompt`' or '`busy_prompt`') and an optional reason ('key' or 'timeout'), written only when given
  - a stop line for a correction records its length as `correction_chars` and never the text; passing text is a TypeError or is ignored, asserted by a test that greps the written file for the token
  - existing tests in tests/`test_audit.py` pass unmodified and an unknown kind is still rejected

### t10 — Update every written description of the first press, and record the amendment

- instruction: Files: docs/shell-integration.md, docs/daemon.md, README.md and the five harness prompt files only. No tilde-home paths in committed docs (harness-smoke fails on them). Wording of the prompt is fixed by decision c21.
- covers: c10, h10, c11, h11, c24, h16, c25, h17, c27, h29
- acceptance:
  - docs/shell-integration.md 'Stopping the agent' and its exit-code table, docs/daemon.md control table, README.md:54 and the stop paragraph in CLAUDE.md, AGENTS.override.md, .pi/SYSTEM.md, AGENTS.colleague.md and QWEN.md all describe the choice prompt, its three keys, the 30 s timeout, stop & correct, and that a session without a terminal stops at once
  - grep for 'press again to kill' finds it only as the line shown after s; scripts/harness-smoke.py --stage config passes; markdownlint-cli2 passes
  - docs/shell-integration.md names the superseded reliable-agent-stop claims (c1, c4, c5, c17, c20, c21, c22, c34), says why the operator asked for the change, and records the 0.13.1 before-state observed on thor on 2026-09-17; .devague/frames/reliable-agent-stop.json has no diff

## Wave 2

### t4 — Daemon busy prompt reads Capabilities.steer instead of `_overrides_steer`

- instruction: Files: nvsh/daemon.py and tests/`test_daemon_stop.py` only. The busy prompt's keys, wording and rules stay exactly as they are (non-goal c15).
- covers: c8, h8
- acceptance:
  - nvsh/daemon.py no longer defines `_overrides_steer`; `_BusyPrompt`.steerable comes from slot.agent.capabilities().steer
  - a daemon test with an openai-compat-like fake (overrides steer(), declares steer=False) gets a busy event whose choices omit 'steer'; with a steer=True fake the choice is offered
  - `cancel_shell`, `kill_shell` and `_force_stop_turn` have no functional diff; tests/`test_daemon_stop.py` stop cases and tests/`test_close_kills_tree.py` pass unmodified

### t5 — Panel: first press opens the choice prompt (keep going, stop, timeout, non-tty)

- instruction: Files: nvsh/panel.py and tests/`test_panel_stop.py`. Replace only the first branch of Panel.`_on_press`; keep `_StopState`'s once-only guards. Read keys with nvsh.promptkeys.`read_choice`, suspending the KeyWatcher the way `_suspending` does. Rewrite the existing first-press tests to the new behaviour rather than deleting them (`test_a_single_press_never_calls_force_stop`, `test_esc_prints_the_stopping_line_within_1s_while_the_source_is_silent`, `test_lone_esc_during_streaming_is_identical_to_sigint`, `test_second_press_calls_force_stop_exactly_once`).
- covers: c2, h2, c3, h3, c6, h6, c9, h9, c26, h18, c28, h19
- acceptance:
  - on a tty the first Ctrl+C or lone Esc during Panel.stream() prints 'nvsh: paused -- \[t\] steer  \[s\] stop  \[Esc\] keep going' within 1 s (ticker paused, thinking/text run closed first) and calls neither cancel nor `force_stop` nor `on_steer` until a key is read -- asserted with recording callables
  - Esc or a 30 s timeout (constant `STOP_PROMPT_TIMEOUT`, patched short in tests) resumes rendering with no events lost or reordered and leaves StreamResult.interrupted False; the outcome is reported through a new `on_choice` callback as '`keep_going`' with reason key or timeout
  - s, or Ctrl+C typed at the prompt, does exactly today's first press: `STOPPING_TEXT` once, cancel once, interrupted True; a further Ctrl+C or Esc calls `force_stop` exactly once and ends the stream within the existing 3 s budget
  - when stdin or stdout is not a tty, TERM=dumb, or the panel is in JSON mode, the first SIGINT cancels at once with no prompt text on stdout or stderr
  - Panel.stream gains `steer_label` ('steer' or 'stop & correct') used verbatim in the legend; choosing t in this task reports 'steer' through `on_choice` and reads no text yet

## Wave 3

### t6 — Panel: correction line, never-mind rules, press at another prompt, turn finished during the prompt

- instruction: Files: nvsh/panel.py and tests/`test_panel_stop.py`. The typed text is handed to the caller through `on_steer`(text) -> bool; the panel does not redact or send anything itself.
- covers: c34, h25, c36, h27
- acceptance:
  - after t the panel reads one line with the existing Panel.`read_tell`; an empty line, Ctrl+C or EOF there means never mind: no callback fires, the turn keeps going and interrupted stays False
  - EOF at the choice prompt itself means keep going and never cancel, `force_stop` or steer
  - a Ctrl+C typed while a proposal or busy prompt is open goes straight to today's stop path and never opens the choice prompt
  - with DONE already queued when the prompt is answered, choosing s still renders the whole answer and the panel reports the stop as `not_running` so callers can leave interrupted False; choosing t returns the text to the caller as a next-request correction

## Wave 4

### t7 — Client: wire the prompt -- label from the capability, redacted steer delivery, audit lines

- instruction: Files: nvsh/client.py and tests/`test_client_stop.py`. Reuse Responder.steer and the `_injector` pattern; do not edit nvsh/`client_transport.py`'s wire format.
- covers: c4, h28, c29, h20, h22
- acceptance:
  - `_stream_request` resolves the target's capability with registry.`steer_capable` and passes `steer_label` to panel.stream on both the daemon and one-shot paths, against a stub daemon that sends no capability information
  - the typed correction is passed through nvsh.redact before Responder.steer; a line containing `HF_TOKEN`=abc123 reaches the adapter redacted and appears nowhere in audit.jsonl
  - each prompt outcome writes exactly one audit stop line: `keep_going` (reason key or timeout), cancel, or steer with origin `stop_prompt` and `correction_chars`; no new daemon control message is added

## Wave 5

### t8 — Client: stop-and-correct sequencing, self-contained correction request, runtime fallback, exit codes

- instruction: Files: nvsh/client.py and tests/`test_client_stop.py`. Rewrite, do not delete: `test_one_shot_first_press_calls_in_process_cancel`, `test_ctrl_c_during_work_exits_130_and_audits_cancel_and_force_kill_once`, `test_single_ctrl_c_exits_130_and_audits_only_cancel`, `test_ctrl_c_at_a_raw_prompt_exits_130_cancels_once_and_never_declines`.
- covers: c22, h14, c23, h15, c30, h21
- acceptance:
  - on a steer=False target, t then a typed line cancels the turn once, waits for it to end (a further press still kills), then streams a second request; `handle_failure`, ask and the slash path no longer return 130 before that follow-up
  - the follow-up AgentRequest.prompt contains the original request text, a statement that the previous attempt was stopped, and the redacted correction, built without reading any harness session state -- asserted for every adapter family fake
  - on a steer=True fake whose steer() returns False, the panel notes it could not steer and asks once whether to stop and correct; yes runs stop-and-correct, anything else discards the text and keeps going; the text is never silently dropped
  - exit status: 0 for keep going and for a delivered steer whose turn ends normally; the follow-up turn's own status after stop-and-correct; 130 only for s (and the kill after it); --json `exit_code` and the audit log agree

## Wave 6

### t9 — Rewrite the cross-family stop test and add an end-to-end pty test of all four outcomes

- instruction: Files: tests/`test_agent_stop.py` and NEW tests/`test_stop_prompt_e2e.py` only.
- covers: c1, h1, c12, h12
- acceptance:
  - tests/`test_agent_stop.py`::`test_two_presses_stop_every_family_on_both_paths` is rewritten as press, s, press and still passes for every adapter family on the daemon and one-shot paths
  - a new pty test drives one turn per outcome -- steer, stop, keep going, timeout -- from a single first press and asserts no adapter call happens before the key
  - a paused client (5 s, patched) against a fake streaming faster than the socket buffer drains loses no events and is not aborted as client-gone
  - no test function that pinned the old first press was deleted without a replacement of the same case; the suite's pass count is not lower than on main

## Wave 7

### t11 — Integrate: full suite, lint gates, version bump and changelog

- instruction: Main-agent task after all merges. Use the version-bump skill (minor).
- covers: c28, h19
- acceptance:
  - uv run pytest -n auto passes; black, isort, flake8, bandit, markdownlint, scripts/scan-secrets.py, teken cli doctor --strict and harness-smoke --stage config all pass
  - git diff main -- nvsh/keys.py is empty and the functional diff of `cancel_shell`/`kill_shell`/`_force_stop_turn` is empty
  - pyproject.toml is bumped (minor) with a CHANGELOG entry naming the prompt, stop & correct, the capability flag and the redaction of the correction line

## End state

### Announcement

- nvsh asks before it stops the agent. While the agent is working, the first Ctrl+C or Esc no longer cancels the turn straight away: the panel pauses and offers a choice -- [t] steer, [s] stop, [Esc] keep going -- so an accidental press costs nothing and the operator can correct the agent instead of killing it. Stopping is still always two keys away, and a terminal that cannot show the prompt stops immediately as before.

### After state

- The first Ctrl+C or Esc pauses the panel and shows 'nvsh: paused -- [t] steer  [s] stop  [Esc] keep going'. Esc (or 30 s of silence) resumes as if nothing happened; s stops exactly as before, with a further press still killing; t takes one line and either steers the running turn (pi, codex) or stops and resends with the correction (every other harness).

### Success signals

- The prompt appears within 1 s of the first press; after [s] the 'stopping…' line appears within 1 s and a further press returns the prompt within 3 s, as in 0.13.0; an unanswered prompt resumes at 30 s (+/- 1 s); keep going and a delivered steer exit 0 with interrupted unset; 100% of registered adapters declare Capabilities.steer and only pi and codex declare it true.

### Terminal tasks

- `t11` — Integrate: full suite, lint gates, version bump and changelog
  - uv run pytest -n auto passes; black, isort, flake8, bandit, markdownlint, scripts/scan-secrets.py, teken cli doctor --strict and harness-smoke --stage config all pass
  - git diff main -- nvsh/keys.py is empty and the functional diff of cancel_shell/kill_shell/_force_stop_turn is empty
  - pyproject.toml is bumped (minor) with a CHANGELOG entry naming the prompt, stop & correct, the capability flag and the redaction of the correction line

### Open items

- [follow_up] Whether real pi and real codex visibly act on a mid-turn steer sent from the stop prompt was not probed on the fleet; only the fakes are exercised. Verify on thor/orin after merge.
- [unknown_nonblocking] The 30 s timeout and the paused panel interact with NVSH_TURN_TIMEOUT (300 s) and with daemon-side buffering while the client renders nothing: confirm in implementation that a paused client cannot stall or drop the harness stream.
- [follow_up] Pre-existing gap outside this frame: operator-typed text from /ask, /steer and the proposal prompt's [t] is sent to the harness unredacted, against CLAUDE.md's 'everything that leaves the process is redacted first'. Needs its own issue.
- [follow_up] Whether real pi and real codex visibly act on a mid-turn steer sent from the stop prompt is only exercised against fakes; probe on thor/orin after merge (frame park v1).
- [unknown_nonblocking] The paused-client assumption (frame c35) was read from nvsh/daemon.py, not measured under a real 6 KB/s reasoning stream; t9's paused-client test is the only evidence until the fleet check.
- [follow_up] Operator-typed text from /ask, /steer and the proposal prompt's [t] is still sent unredacted; this plan redacts only the new correction line. Needs its own issue (frame park v3).
- [unknown_nonblocking] t5 and t6 both edit nvsh/panel.py and tests/test_panel_stop.py, and t7 and t8 both edit nvsh/client.py and tests/test_client_stop.py: they are dependency-ordered on purpose and must never be run in the same wave.
